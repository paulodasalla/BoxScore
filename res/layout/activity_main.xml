<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/backdefa"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.paulodasalla.boxscore.MainActivity" >

    <Button
        android:id="@+id/btnDisplayMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="142dp"
        android:text="News" 
        android:onClick="buttonBtn"
        />
    

    <Button
        android:id="@+id/btnDisplayScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btnDisplayMessage"
        android:layout_alignBottom="@+id/btnDisplayMessage"
        android:layout_toRightOf="@+id/btnDisplayMessage"
        android:text="Scores" 
        android:onClick="buttonBtn2"
        />

    <Button
        android:id="@+id/btnDisplayStandings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/btnDisplayScore"
        android:layout_toRightOf="@+id/btnDisplayScore"
        android:text="Standings" />

</RelativeLayout>
